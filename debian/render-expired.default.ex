# Defaults for render-expired initscript
# sourced by /etc/init.d/render-expired
# installed at /etc/default/render-expired by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
